[project]
name = "learning-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Muhammad Wasil Ali", email = "wasilali4999@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=4.0.3",
    "python-dotenv>=1.2.3",
    "supabase>=2.31.0",
]

[project.scripts]
learning-mcp = "learning_mcp:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
